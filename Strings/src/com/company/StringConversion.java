package com.company;

public class StringConversion {

	// to covert from string to primitive types

	// we use parseInt() parseFloat() .....
	// or Integer.valueOf()
	// parseInt returns int (primitive value) where as Integer.valueOf()return
	// wrapper

	// to convert from primitive to string we use toString
	// or valueOf() toString works for object only while value of works for
	// primitives and object

}
